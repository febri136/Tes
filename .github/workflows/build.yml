name: Build postmarketOS msm89x7 Nightly

on:
  workflow_dispatch: # Memicu build secara manual lewat tab Actions
  schedule:
    - cron: '0 2 * * 5' # Otomatis build setiap hari Jumat jam 02:00 UTC

jobs:
  build-image:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Install System Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y git python3 openssl qemu-user-static

    - name: Install pmbootstrap from Official Git
      run: |
        # Mengunduh langsung dari server utama postmarketOS
        git clone --depth=1 https://gitlab.postmarketos.org/postmarketOS/pmbootstrap.git
        
        # MEMASUKKAN PATH: Daftarkan folder pmbootstrap ke dalam environment path GitHub Actions
        echo "$GITHUB_WORKSPACE/pmbootstrap" >> $GITHUB_PATH

    - name: Verify pmbootstrap Installation
      run: |
        # MEMANGGIL LANGSUNG: Menggunakan python3 agar modul internal pmbootstrap terbaca sempurna
        python3 pmbootstrap/pmbootstrap.py --version

    - name: Generate pmbootstrap v3 Configuration File
      run: |
        # Menggunakan 'yes' tanpa batas untuk mengisi semua opsi default secara otomatis tanpa galat EOF
        yes "" | python3 pmbootstrap/pmbootstrap.py init || true

        # REVISI: Menyuntikkan konfigurasi msm89x7 target (Opsi 'channel' dan 'vendor' yang usang telah dihapus)
        python3 pmbootstrap/pmbootstrap.py config device qcom-msm89x7
        python3 pmbootstrap/pmbootstrap.py config ui phosh
        python3 pmbootstrap/pmbootstrap.py config mirrors.alpine http://alpinelinux.org
        python3 pmbootstrap/pmbootstrap.py config mirrors.postmarketos http://postmarketos.org

    - name: Zap and Pull Mainline Packages
      run: |
        # Menginisialisasi folder kerja secara otomatis dan sah dengan struktur metadata bawaan sistem
        python3 pmbootstrap/pmbootstrap.py zap
        
        # Memperbarui pmaports menggunakan eksekusi langsung Python3
        python3 pmbootstrap/pmbootstrap.py pull

    - name: Build Rootfs Image
      run: |
        # Proses pembuatan citra sistem operasi tanpa enkripsi awal (FDE)
        python3 pmbootstrap/pmbootstrap.py install --no-fde

    - name: Export Deployment Images
      run: |
        mkdir -p ./output_artifacts
        # Mengekspor hasil kompilasi citra (img) ke folder output
        python3 pmbootstrap/pmbootstrap.py export --output=./output_artifacts

    - name: Upload Build Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: postmarketOS-qcom-msm89x7-edge
        path: ./output_artifacts/*
        
